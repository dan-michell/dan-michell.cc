---
import LinkIcon from '../common/LinkIcon.astro'

interface Props {
	
}

const link_props = [
	{
		href: "",
		iconName: "github", 
		size: "size-6"
	}
]
---
<div>
	{link_props.map((iconProps) => {
	return <LinkIcon {...iconProps} />
	})}
</div>
